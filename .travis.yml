matrix:
  include:
    - language: python
      python:
        - "3.8"
      install:
        - "pip install pipenv"
        - "cd log-scraper && pipenv install --dev"
      script:
        - "python -m unittest"
